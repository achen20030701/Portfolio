# 虚拟环境（太大，不要上传）
.venv/
venv/

# 环境变量（有密码，绝对不能上传）
.env

# Python 缓存（自动生成，不需要上传）
__pycache__/
*.pyc

# 数据文件（运行时生成）
data/uploads/
data/chroma/
*.db

# IDE 配置（每个人的不一样）
.vscode/
.idea/

# 系统文件
.DS_Store
Thumbs.db

# Node.js 依赖（太大，不要上传）
node_modules/

# 打包文件（不要上传）
*.zip
